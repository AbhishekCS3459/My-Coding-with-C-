#include<bits/stdc++.h>
using namespace std;
getminmax(int arr[],int *ptr,int n)
{
    int minNo=INT_MAX;
    int maxNo=INT_MIN;
    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    
}
int main(int argc, char const *argv[])
{
    int n;
    cin>>n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin>>arr[i];
    }
    int *ptr=arr;
    cout<<getminmax(arr,n);
    return 0;
}
